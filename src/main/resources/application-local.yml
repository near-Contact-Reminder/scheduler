spring:
  application:
    name: scheduler
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:17254/NEAR
    username: root
    password: test
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true

swyp:
  firebase:
    key-path: 'static/firebase.json'
    url: 'https://fcm.googleapis.com/v1/projects/near-3cbf4'
